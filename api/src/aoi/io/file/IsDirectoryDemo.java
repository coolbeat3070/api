package aoi.io.file;
/*
 * java.io.File 의 메소드인
 * isFile() // 해당 파일이 있는 지 확인..없으면 false
 * isDirectory() // 해당 패스에 디렉토리(폴더)가 존재하는지 .없으면 false
 * */
import java.io.File;

public class IsDirectoryDemo {
	public static void main(String[] args) {
		// import 단축키
		String directory = "c:/temporary.txt";
	//	File file = new File(source +"");
	}
}
